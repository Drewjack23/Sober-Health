{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Run Vite Dev Server",
      "cwd": "${workspaceFolder}",
      "command": "npm run dev -- --host --strictPort --port 5173 --open"
    }
  ]
}
