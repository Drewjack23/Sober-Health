import { Ionicons } from '@expo/vector-icons';
import { LinearGradient } from 'expo-linear-gradient';
import { router } from 'expo-router';
import { ScrollView, StyleSheet, View } from 'react-native';
import { Text } from '@/components/AppText';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Button } from '@/components/ui';
import { colors, gradients, radius, spacing } from '@/constants/theme';
import { useApp } from '@/state/AppProvider';

export default function OnboardingResult() {
  const { data, completeOnboarding } = useApp(); const a = data.onboarding;
  const start = () => { completeOnboarding(); router.replace('/(tabs)'); };
  const items = [{ icon: 'barbell-outline' as const, label: 'Fitness goal', value: a.goals.slice(0, 2).join(' + ') || 'Build consistency' }, { icon: 'restaurant-outline' as const, label: 'Nutrition focus', value: a.diet || 'Balanced variety' }, { icon: 'heart-outline' as const, label: 'Wellness focus', value: a.wellnessAreas.slice(0, 2).join(' + ') || 'Overall wellbeing' }, { icon: 'shield-checkmark-outline' as const, label: 'Recovery support', value: a.recoveryChoice === 'yes' ? (a.recoveryCategory || 'Included privately') : 'Available whenever you want it' }];
  return <LinearGradient colors={gradients.dark} style={styles.safe}><SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content}><View style={styles.check}><Ionicons name="checkmark" size={34} color={colors.white} /></View><Text style={styles.eyebrow}>YOUR PLAN IS READY</Text><Text style={styles.title}>A healthier path,{`\n`}built around you.</Text><Text style={styles.subtitle}>Nothing here requires perfection. We’ll help you notice progress, adapt, and keep going.</Text><View style={styles.card}>{items.map((item, index) => <View key={item.label} style={[styles.item, index < items.length - 1 && styles.itemBorder]}><View style={styles.icon}><Ionicons name={item.icon} size={20} color="#BCA7FF" /></View><View style={styles.itemCopy}><Text style={styles.label}>{item.label}</Text><Text style={styles.value}>{item.value}</Text></View></View>)}</View><View style={styles.quote}><Ionicons name="sparkles" size={18} color="#BCA7FF" /><Text style={styles.quoteText}>{a.motivation ? `“${a.motivation}”` : 'Your health is bigger than a number.'}</Text></View><Button label="Start my journey" icon="arrow-forward" onPress={start} style={styles.button} /></ScrollView></SafeAreaView></LinearGradient>;
}
const styles = StyleSheet.create({ safe: { flex: 1 }, content: { flexGrow: 1, justifyContent: 'center', padding: spacing.xl, maxWidth: 600, width: '100%', alignSelf: 'center' }, check: { width: 52, height: 52, borderRadius: radius.lg, alignItems: 'center', justifyContent: 'center', backgroundColor: colors.purple, marginBottom: 21 }, eyebrow: { color: '#A99FE0', fontSize: 10, fontWeight: '600', letterSpacing: 1.6 }, title: { color: colors.white, fontSize: 33, lineHeight: 40, fontWeight: '700', letterSpacing: -0.75, marginTop: 8 }, subtitle: { color: '#B4AFC7', fontSize: 15, lineHeight: 23, marginTop: 9 }, card: { borderTopWidth: 1, borderBottomWidth: 1, borderColor: '#FFFFFF18', paddingHorizontal: 2, marginTop: 26 }, item: { flexDirection: 'row', alignItems: 'center', gap: 13, paddingVertical: 15 }, itemBorder: { borderBottomWidth: 1, borderBottomColor: '#FFFFFF12' }, icon: { width: 36, height: 36, borderRadius: radius.md, backgroundColor: '#8B5CF61A', alignItems: 'center', justifyContent: 'center' }, itemCopy: { flex: 1 }, label: { color: '#8F89A6', fontSize: 10, fontWeight: '600', textTransform: 'uppercase', letterSpacing: 0.9 }, value: { color: colors.white, fontSize: 14, fontWeight: '600', marginTop: 3 }, quote: { flexDirection: 'row', gap: 9, alignItems: 'center', marginVertical: 21, paddingHorizontal: 3 }, quoteText: { color: '#D3CEE3', fontSize: 13, fontStyle: 'italic', flex: 1, lineHeight: 20 }, button: { marginTop: 1 } });
